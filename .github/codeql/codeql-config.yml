name: Farkle CodeQL config

# The Kiota API client is generated (never hand-edited — see docs/api-client-generation.md), so
# excluding it keeps generated-code style findings (e.g. missing doc-comment summaries) out of the
# results. Application code is still fully analysed.
paths-ignore:
  - src/Farkle.ApiClient
