@inherits BlazorState.BlazorStateComponent

<MudDropContainer Class="drop-zones"
                  ItemDropped="ItemUpdatedAsync"
                  Items="GameState.DiceInPlay"
                  ItemsSelector="@((item, dropzone) => item.Identifier == dropzone)"
                  T="DraggableDie">
  <ChildContent>

    <MudDropZone Class="d-flex flex-grow-1 flex-row flex-wrap justify-center rounded zone"
                 Style="padding: 5rem 1rem 1.25rem;"
                 Identifier="Rolled"
                 T="DraggableDie">
      <MudText Style="position:absolute; top:0.75rem; left:0; right:0; text-align:center; pointer-events:none;"
               Color="Color.Primary" Typo="Typo.h6">
        In Play
      </MudText>
    </MudDropZone>

    <MudDropZone Class="d-flex flex-grow-1 flex-row flex-wrap justify-center rounded zone"
                 Style="padding: 5rem 1rem 1.25rem; border: 2px dashed #FFE600;"
                 Identifier="SetAside"
                 T="DraggableDie">
      <MudText Style="position:absolute; top:0.75rem; left:0; right:0; text-align:center; pointer-events:none;"
               Color="Color.Primary" Typo="Typo.h6">
        Set Aside 🎯
      </MudText>
    </MudDropZone>

  </ChildContent>

  <ItemRenderer>
    <MudItem Class="align-center d-flex justify-center"
             Style="margin: 1.75rem 0.5rem 0.75rem;">
      <Die DieValue="context.Value"
           IsDragging="context.IsDragging"
           Size="36">
      </Die>
    </MudItem>
  </ItemRenderer>
</MudDropContainer>