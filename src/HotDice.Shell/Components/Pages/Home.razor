@page "/"

<h1>HotDice</h1>

<p>
    Shell smoke screen. It renders a <code>Blazor.Dice</code> component — an existing Razor Class
    Library, unchanged — to prove the shared UI runs inside the WebView. The real game UI arrives
    when the client UI moves into a shared RCL (docs/mobile-strategy.md, Phase 1).
</p>

<Die DieValue="DieValue.Five" Animate="false" />
