<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is permitted ONLY to the Android emulator's host loopback (10.0.2.2) and localhost,
  so a CI/dev build can reach a locally-run backend over http (device tests, #339). Every other host
  stays HTTPS-only (the platform default). This is safe to ship: 10.0.2.2 is a magic address that only
  resolves to the developer machine from inside an emulator, so a real device on a real network can't
  use it. The production backend is https, so normal traffic is unaffected.
-->
<network-security-config>
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="false">10.0.2.2</domain>
    <domain includeSubdomains="false">localhost</domain>
  </domain-config>
</network-security-config>
