<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <!-- Thin shared kernel: pure shared domain types (e.g. ScoreCalculator), referenced by
         both the Farkle domain and the WASM client. NO infrastructure dependencies — the
         FastEndpoints/Eventuous plumbing that used to live here moved into Farkle. -->

</Project>
